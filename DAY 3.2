Understand about  Import and export in react.js
---->                                          
                                          Import---- Export
1.	Default Export : A file can have only one default export. Default Import: When importing a default export, you can name the import whatever you like.
2.	Named Export: A file can have multiple named exports. Each named export must be explicitly exported.
     Named Import: When importing named exports, the import names must match the export        names exactly. Named imports must be enclosed in curly braces.
3.	Combined export import
   // [vite] Failed to reload /src/App.jsx. This could be due to syntax errors or importing non-existent modules. (see errors above)
error @ client:836Understand this error 
NOTE :----   code  is running fine but in console it is giving error……….. so for that we write 
Before 
import { NetflixSeries, Footer , Header } from "./components/NetflixSeries";

After
                 (it is default (NetflixSeries)) and we can give any name to default…
import NetflixSeries,{ Footer , Header } from "./components/NetflixSeries";

code: -------------------------------------------------------------------------------------------------------------------------------------
App.jsx file
// 1->  import NetflixSeries from "./components/NetflixSeries";

// 2->                           // 3
//import {NetflixSeries, Footer , Header } from "./components/NetflixSeries"; // first letter must be capital.**
// 3 solution  to solve error
import NetflixSeries,{ Footer , Header } from "./components/NetflixSeries";

export const App = () => {
   
   return (
    <>
        <Header/> {/*  3 part of export import */}
        < NetflixSeries />    
        < NetflixSeries />
        < NetflixSeries />
        < NetflixSeries />
        < NetflixSeries />
        <Footer/>
    </>    
    );
};

// Netflixseries.jsx
// 2 named Export
// (2 way ) export const NetflixSeries = () => { 
  export const NetflixSeries = () => { 
    const name="Queens of tear";
    const rating = 8.2;
    let age = 19;
    let canWatch = "Not Available";
    if(age >= 18) canWatch= "Watch Now";
    const summary =
    "Queen of Tears follows Hong Hae-in (Kim Ji-won), a chaebol heiress and Baek Hyun-woo (Kim Soo-hyun), the son of farmers, as their loveless marriage faces a crisis, leading them to discover Hae-in's terminal illness and rekindle their romance amidst chaos and betrayal.";
    const returnGenre = () => {
        const genre = "science";
        return genre;
    };
    // 4 way  with function and can pass parameter in dynamic....
    const returncanwatch =() => {
      if(age>=18) return " Watch now";
      return " Not Available";
    }
 return (
     <div>
        <div> 
            <img src="642647.jpg" alt="img_poster" 
            width="20%" height ="20%"></img>
         </div>
     <h2>Name: {name} </h2>
     <h3>Rating: {5+2} </h3>
     <p>Summary:{summary}</p>
     <p> Genre: {returnGenre()} </p> {/* calling function*/}
     {/* 2 way with use of ternary operator */}
     {/* <button> {age >= 18 ? "Watch Now" : "Not Available"}</button> */}
     {/* 3 Way */}
     {/* <button>{canWatch}</button> */}
     {/* 4 way */}
     <button>{returncanwatch()}</button>
     </div>
   );
};
// 1 default export...(1 way of import export)
 // here given permission that we have exported now any body  can import this.
//export default NetflixSeries;

//2  named export(way of import export)
//  export const Footer = () => {
//   return <p>Copyright @sarvagya_admin</p>;
//  };

 // 3 combined import export(3 way)

 // error is coming so to solve that.... (see App.jsx file) we do not write this in curly bracket..in import
export default NetflixSeries; // here default way ( 1 time only we can  use )

export const Header = () => {
      return <p>@copyright_ starter</p>;
};
 export const Footer = () => { // here named  way (as much you want)
     return <p>Copyright @sarvagya_admin</p>;
  };
-----------------------------------------------------------------------------------------------------------------------------------------------------------



 


